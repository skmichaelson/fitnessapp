<main class="group content">
  <% @goal.attributes.each do |attr| %>
    <li><%= attr[0].gsub("_", " ").capitalize %>: <%= attr[1]%></li>
  <% end %>

  <%= link_to "<button class='button'>Update your goals</button>".html_safe,
    edit_goal_url(current_user.goal) %>
</main>