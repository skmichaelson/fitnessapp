<%= render 'demo/flashes' %>

<main class="group content">

  <%= render 'shared/errors' %>
  <%= render 'shared/flashes' %>

  <h1>User Index Page</h1>

  <%= render @users %>
  <%= paginate @users %>

</main>